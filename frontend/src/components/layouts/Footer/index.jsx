import React from "react";
import { Box, Typography } from "@mui/material";

export default function Footer() {
    return (
        <Box className="footer-root">
            <Typography className="footer-text">© Football Bet — For entertainment only</Typography>
        </Box>
    );
}
